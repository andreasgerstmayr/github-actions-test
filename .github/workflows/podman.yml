name: Podman test
on:
  workflow_dispatch:

jobs:
  bundle:
    runs-on: ubuntu-22.04
    steps:
      - name: Create container
        run: |
          podman version
          apt-cache policy podman

          echo regular user
          export

          echo sudo
          sudo bash -c export

          cat <<EOF | sudo podman --log-level debug build -t test-container -f - .
          FROM fedora:36
          RUN echo test
          EOF
